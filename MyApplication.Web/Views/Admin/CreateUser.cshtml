@model MyApplication.Web.Models.User
@{
    ViewData["Title"] = "Yeni Kullanıcı Oluştur";
}
<div class="container mt-5">
    <h2 class="mb-4">Yeni Kullanıcı Oluştur</h2>
    <form asp-action="CreateUser" method="post">
        <div class="mb-3">
            <label asp-for="UserName" class="form-label">Kullanıcı Adı</label>
            <input asp-for="UserName" class="form-control" />
            <span asp-validation-for="UserName" class="text-danger"></span>
        </div>
        <div class="mb-3">
            <label asp-for="Email" class="form-label">Email</label>
            <input asp-for="Email" class="form-control" />
            <span asp-validation-for="Email" class="text-danger"></span>
        </div>
        <div class="mb-3">
            <label asp-for="Password" class="form-label">Şifre</label>
            <input asp-for="Password" class="form-control" type="password" />
            <span asp-validation-for="Password" class="text-danger"></span>
        </div>
        <div class="mb-3">
            <label asp-for="DateOfBirth" class="form-label">Doğum Tarihi</label>
            <input asp-for="DateOfBirth" type="date" class="form-control" />
            <span asp-validation-for="DateOfBirth" class="text-danger"></span>
        </div>
        <div class="mb-3">
            <label asp-for="PhoneNumber" class="form-label">Telefon Numarası</label>
            <input asp-for="PhoneNumber" class="form-control" />
            <span asp-validation-for="PhoneNumber" class="text-danger"></span>
        </div>
        <div class="mb-3 form-check">
            <input asp-for="IsSuperUser" class="form-check-input" type="checkbox" />
            <label asp-for="IsSuperUser" class="form-check-label">Yönetici (SuperUser)</label>
        </div>
        <button type="submit" class="btn btn-success">Oluştur</button>
        <a href="@Url.Action("Users", "Admin")" class="btn btn-secondary ms-2">Geri Dön</a>
    </form>
</div>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
} 